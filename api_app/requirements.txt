# api_app/requirements.txt — pinned set used by the api / worker / beat sidecars.

fastapi==0.115.5
uvicorn[standard]==0.32.0
httpx==0.27.2
pyjwt[crypto]==2.10.0
websockets==13.1

# Celery + Redis broker
celery==5.4.0
redis==5.2.0

# Azure SDK surface — covers everything api/services/* uses
azure-identity==1.19.0
azure-storage-blob==12.23.1
azure-data-tables==12.5.0
azure-keyvault-secrets==4.9.0
azure-mgmt-resource==23.2.0
azure-mgmt-storage==21.2.1
azure-mgmt-keyvault==10.3.1
azure-mgmt-compute==33.0.0
azure-mgmt-network==27.0.0
azure-mgmt-containerregistry==10.3.0
azure-mgmt-containerservice==32.1.0
azure-mgmt-authorization==4.0.0
azure-mgmt-msi==7.0.0
azure-mgmt-monitor==6.0.2

# Kubernetes Python client (used by services.monitoring k8s_* helpers)
kubernetes==31.0.0

# Pydantic for any remaining model usage
pydantic==2.9.2
