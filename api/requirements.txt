# Azure Functions Python v2 + Durable Functions
azure-functions==1.21.3
azure-functions-durable==1.2.10

# Azure SDK
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0
azure-mgmt-keyvault==10.3.0
azure-mgmt-authorization==4.0.0
azure-mgmt-resource==23.2.0
azure-mgmt-network==28.0.0
azure-mgmt-compute==33.0.0
azure-mgmt-storage==21.2.1
azure-storage-blob==12.23.1
azure-mgmt-containerregistry==10.3.0
azure-containerregistry==1.2.0
azure-mgmt-containerservice==34.0.0
azure-mgmt-msi==7.0.0

# JWT validation
PyJWT[crypto]==2.9.0

# SSH connectivity (VM command execution — much faster than Run Command)
paramiko>=3.4.0

# HTTP client (explicit — used by prepare_blast_db for NCBI S3 access)
requests>=2.31.0
cryptography==43.0.3

# Models
pydantic==2.9.2

# HTTP
httpx==0.27.2
pyyaml
